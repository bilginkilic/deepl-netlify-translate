# Render Dashboard → Environment Variables altında tanımlayın.
# Bu dosyayı .env olarak kopyalayıp yerel dev:render için kullanabilirsiniz.

# SEO API (zorunlu — /api/seo-optimize için)
ANTHROPIC_API_KEY=sk-ant-api03-...

# SEO API (isteğe bağlı)
# SEO_MODEL=claude-haiku-4-5-20251001
# SEO_MAX_TOKENS=8192

# Genel (isteğe bağlı)
# CORS_ORIGIN=*
# DEEPL_API_URL=https://api.deepl.com/v2/translate

# PORT — Render otomatik atar; yerelde dev:render 3000 kullanır
